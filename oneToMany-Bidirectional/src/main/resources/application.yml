#http://localhost:8281/swagger-ui/index.html
spring:
  application:
    name: on-to-many
  datasource:
    url: jdbc:postgresql://localhost:5432/one-to-many
    username: postgres
    password: admin
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8281